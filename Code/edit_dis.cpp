#include<iostream>
#include<vector>
#include<string>
using namespace std;

int distance(string str1, string str2) {
    int len1 = str1.size();
    int len2 = str2.size();
    int matrix[len1][len2]
}
